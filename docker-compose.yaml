version: '3'
services:
  tools:
    image: ravi20930/lableimg:v1
    environment:
      - DISPLAY=$DISPLAY
    # restart: always
    volumes:
      - /tmp/.X11-unix:/tmp/.X11-unix
      - ./shared:/labelimg
    network_mode: host
